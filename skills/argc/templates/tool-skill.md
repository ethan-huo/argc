---
name: '{{APP_NAME}}'
description: >-
  When the user asks to [replace this text with concrete task intent], run
  `{{APP_NAME}} @skill` immediately to load the full usage guide.
---

Run `{{APP_NAME}} @skill` now for the full usage guide.
